# LangGraph-Tool-Calling-Agent
A minimal tool-calling agent built from LangGraph primitives (StateGraph, ToolNode, tools_condition) rather than prebuilt wrappers, so the agentic loop is explicit and easy to follow.
